#include "include/backgrounds.h"

void whoWillCome(string PlayerA, string PlayerB)
{ 

    if(PlayerA == "Kiana")
    memcpy(&Playera, &Kiana, sizeof(Kiana));
    if(PlayerA == "Mei")
    memcpy(&Playera, &Mei, sizeof(Mei));
    if(PlayerA == "Fuhua")
    memcpy(&Playera, &Fuhua, sizeof(Fuhua));
    if(PlayerA == "Theresa")
    memcpy(&Playera, &Theresa, sizeof(Theresa));
    if(PlayerA == "Bronya")
    memcpy(&Playera, &Bronya, sizeof(Bronya));
    if(PlayerA == "Himeko")
    memcpy(&Playera, &Himeko, sizeof(Himeko));
    if(PlayerA == "Sakura")
    memcpy(&Playera, &Sakura, sizeof(Sakura));
    if(PlayerA == "Liliya")
    memcpy(&Playera, &Liliya, sizeof(Liliya));
    if(PlayerA == "Rosaliya")
    memcpy(&Playera, &Rozaliya, sizeof(Rozaliya));
    if(PlayerA == "Rita")
    memcpy(&Playera, &Rita, sizeof(Rita));
    if(PlayerA == "Kallen")
    memcpy(&Playera, &Kallen, sizeof(Kallen));
    if(PlayerA == "Seele")
    memcpy(&Playera, &Seele, sizeof(Seele));
     
    if(PlayerB == "Kiana")
    memcpy(&Playerb, &Kiana, sizeof(Kiana));
    if(PlayerB == "Mei")
    memcpy(&Playerb, &Mei, sizeof(Mei));
    if(PlayerB == "Fuhua")
    memcpy(&Playerb, &Fuhua, sizeof(Fuhua));
    if(PlayerB == "Theresa")
    memcpy(&Playerb, &Theresa, sizeof(Theresa));
    if(PlayerB == "Bronya")
    memcpy(&Playerb, &Bronya, sizeof(Bronya));
    if(PlayerB == "Himeko")
    memcpy(&Playerb, &Himeko, sizeof(Himeko));
    if(PlayerB == "Sakura")
    memcpy(&Playerb, &Sakura, sizeof(Sakura));
    if(PlayerB == "Liliya")
    memcpy(&Playerb, &Liliya, sizeof(Liliya));
    if(PlayerB == "Rosaliya")
    memcpy(&Playerb, &Rozaliya, sizeof(Rozaliya));
    if(PlayerB == "Rita")
    memcpy(&Playerb, &Rita, sizeof(Rita));
    if(PlayerB == "Kallen")
    memcpy(&Playerb, &Kallen, sizeof(Kallen));
    if(PlayerB == "Seele")
    memcpy(&Playerb, &Seele, sizeof(Seele));
        
    if(PlayerA == PlayerB)
    {
        cout<<"Competitors Failure. Enter '1' to continue. Enter '2' to exit.\n";
        string a;
        cin>>a;
        if(a == "李锗均最帅，我要一起来！")
            {}
            else 
            return;

    }




}